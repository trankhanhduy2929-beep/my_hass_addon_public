#include <tunables/global>

profile ecovacs_cn_backend flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,

  /init rix,
  /package/** rix,
  /command/** rix,
  /etc/s6-overlay/** rix,
  /etc/services.d/** rix,
  /etc/cont-init.d/** rix,
  /etc/cont-finish.d/** rix,
  /run/** rwkix,
  /var/run/** rwkix,
  /run.sh rix,
  /usr/** rix,
  /usr/local/lib/python3.14/site-packages/** mr,
  /lib/** rix,
  /bin/** rix,
  /etc/ssl/** r,
  /etc/resolv.conf r,
  /etc/hosts r,
  /etc/nsswitch.conf r,
  /dev/null rw,
  /dev/random r,
  /dev/urandom r,
  /dev/tty rw,
  /proc/** r,
  /sys/class/net/** r,
  /tmp/** rwk,
  /data/** rwk,

  deny /home/** rwklx,
  deny /root/** rwklx,
}
